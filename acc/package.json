{
  "name": "acc",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compile": "truffle compile",
    "migrate": "truffle migrate --reset --network main_fork",
    "test": "truffle test --network main_fork"
  },
  "dependencies": {
    "@openzeppelin/test-helpers": "^0.5.6",
    "@truffle/hdwallet-provider": "^1.2.0",
    "@uniswap/v2-core": "^1.0.1"
  }
}
